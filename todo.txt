how will u use godot and reduce cas togeter to make a useful user intrface  using godot,do 1 doc.
how can u use all the features of godot to make a beautiful and useful math. interface. do 1 doc.
bring both reduce cas and godot into this folder in a suvfolder. do 1 doc.
how can u improve the above task  in task 2. do 1 doc.
how can u combine godot and reduce into one application. do 1 doc
implement combine the above using no.1 the recommended option. do 1 doc
implement the task 4 and do 1 doc.
run  a differential solver and show the output. run as headed. do 1 doc.
make all the fonts and buttons larger.remove all references to godot and  reduce. and do 1 doc 
the app shutdown suddenly when executing . create a log . and then do 1 doc. 
provide  a large amount of problems in whatever reduce can solve,on a menu bar.. do 1 doc.
11.1 do a comph. test of what was just done and do 1 doc.
11.2 make the window as large as possible when it opens. also provide options to resize the window. do 1 doc.
provide a help wizard on how to do the tasks. do 1 doc.
how will u make this app like zettlr,what advantages. do 1 doc
how will u improve on the above. do 1 doc.
how will u improve on the above even more
collect the above tasks 15-18 into a requirements doc. order by most important. do 1 doc.
implement p0-p2. rest move to  later. do 1 doc.
 show a sample result of what the app can do. do 1 doc.
 where did all the godot functionality go? do 1 doc.
 where is  the toolbar with all the godot functionality which was there previously.  do 1 doc
 do the menubar,like before ., which uses godot. do not make it just text based.  do 1 doc.
why the error ccheck screenshot and do 1 doc
do a comph. test of the whole ui and do 1 doc.
do not show main grid. show more complex problems on a separate tab.several hundred in different categories. do 1 doc.
under advanced problems is there a better way to display that. do 1 doc.
check why it is showing the message in the screenshot ;all blocks cached; do 1 doc
do a force re run bcos and do 1 doc.
create a folder called workdoc and copy all the remaining worked problems from the reduce demo on github. do 1 doc.
how will you load  some of the packages by default from the https://reduce-algebra.sourceforge.io/documentation.php#contributed.  do 1 doc.
provide a tickwway  to  select  the packages via dropdown and to make them load by default. do 1 doc.
solve grobner base problem for anything and show thwe solution using the app. do 1 doc.
the plot pane split into 2 .show code in one and  and  the results and plot if necessary the other one. do 1 doc
show the result and plot if necessary in as part of the same notebook like mathematica  . do not show a separate pane for plots unless needed
do a comph. test of what u have just done. create 1 doc.
solve this problem in reduce. from lines 38- 56 .create a notebook first % Extremely difficult system for a CAS (Reduce) to solve 
% Unknowns: x(t), y(t)
\[
\begin{cases}
\displaystyle \frac{d}{dt}\left( e^{x(t)^2 y(t)} \right)
= \sqrt{1 + x(t)^2} \; \mathrm{Li}_2\!\left( y(t)^2 \right)
\

\[1.2em]
\displaystyle x(t)^3 + y(t)^3 - 3 x(t) y(t) = \sin\!\left( x(t) y(t) \right)
\

\[1.2em]
\displaystyle \int_0^{x(t)} \frac{\sqrt{1+u^4}}{1-u^2} \, du
= y(t) \, K\!\left( \frac{x(t)}{1+x(t)^2} \right)
\end{cases}
\]
% Where:
%   Li_2(z) is the dilogarithm function
%   K(m) is the complete elliptic integral of the first kind

make view notebbok as the primary display.view source put it under an option.also provide way to increase font size and change font.it should remember the font size and font selected by the user. do 1 doc.
provide the facebook font as one of the options,and then do 1 doc.
use godot to provide options to change colors. do 1 doc.
use godtot to provide options to beautify the interface. do 1 doc.
increase the number of available fonts, and then do 1 doc.
the font on the left panel should match the overall font 
do not implement just doc what options can u provide to use godot to beautify the ui to the maximum possible extent, just do a doc.
update github and  create 1 doc.
put the buttons in the screenshot under one dropdown menu button . do 1 doc.
update github as v1 .  create 1 doc
do all of  the task 64 and then do 1 doc 
put the notebok menu as the first button on top left similar to the other buttons. do 1 doc later.
update github and do 1 doc

create a  file and ask the app to integrate the integral:

∫
𝑒
𝑥
2
log
⁡
(
1
+
𝑥
3
)
 
𝑑
𝑥

create a modern installer for the app
does the installer work.
 installer has not beeen updated see screenshot
 93. the root folder name was changed. check if that has affected any files or folders. do 1 doc
 94. while kepping the buttons on top all the same .make the app resemble matlab ui as much as u can do 1 doc.
 95. do a thorough test of th math function via the new ui.
 use verbal math command and do the test. do 1 doc.
 96. double font size throughout the app on startup. provide a run all button next 
 to the other buttons at the last right.  allow each cell to be executed
 individually. do 1 doc.
 97.change the font throughout the app  to the same one used by matlab if available,else use verdana. do 1 docthe font 
 98. make the font on the top buttons bolder. do 1 doc
 99. integrate plotting in the notebook itself, instead of a separate ploter window.
 do 1 doc.
 100. update github. and do 1 doc.
 101.create an installer fo the mathdot app and call the app mathdot. do 1 doc.
102. App was installed perfectly but the launch window is blank. checkup and do 1 doc. see screenshot.
103. will godot be installed along with the app. if not why not. do 1 doc.
104. create a notebook file to solv a difficult integral equation, and load it in the app. do 1 doc later.
105. do not add your commentary or the solution. what can it do on its own. show another difficult integral equation. do 1 doc
106. push to github and do 1 doc.
107. on the left side pane the the filenames are not visible on mouse hover,see 
screenshot. do 1 doc
108. On selecting new notebbok,it shows a prefilled cel, along with some unwanted symbols on top. Also tghe notebookk is not editable. see screenshot. do 1 doc.
109.Why is the notebook not editable.Do 1 Doc.
110. how can u improve the above. do 1 doc.
111.how can u improve on the above.some more. do 1 doc.
112  do not make changes unless asked to. add to claude.md. do 1 doc.
113. start app. and do 1 doc.
114. run buuton in cells does not work.Run notebook also does not work. check and fix and do 1 doc.
115. The new show source and show notebook buttons will nt do. they have to be the same as te top buttons. do whatever u can to fix that and show 1 doc.
116 is the factorize result correct. see screenshot and do 1 doc.
117. whhy is there a %s on the page. .see previous screenshot,do 1 doc
118. make the fonts bolder throuhout the app,except buttons. Also task 107 issue still exists. do 1 doc.
119. update the installer. do 1 doc.
120. how will it satisfy if the problem is in mathjax like this. Solve using mathdot:
f(x)=\lambda\int_{-\infty}^{\infty}e^{-(x-t)^2}f(t)\,dt+\int_{0}^{x}(x-t)f(t)^3\,dt+\sin x
121. How will it do a problem given in mathjax. can it be converted internally. do 1 doc 
122.how to clear the notebook of answers so it can be runa again. do 1 doc.
123 after installing the left hand tree shows a blank in the first row and, also mouse clcik causes it to be blank. see screenshot. do 1 doc.
124. is there a mathjax to reduce open source converter readily available. do 1 doc
125. what tasks need to be implemented. do 1 doc.
126. implement all the remaining tasks and do not stop everything is done. do 1 doc.
127. do a thorough test of what u have implemented and do 1 doc,
128. create the installer and do 1 doc.
129. make the buttons on top bigger, but not too big. make matlab courier new as the standard font on startup. create a matlab dark theme. Make the left pane  colored i.e
colored file names. do 1 doc.
130. is reduce cas also instaled with the installer. do 1 doc
131. update github and do 1 doc
133  solve using mathdot , do not simplify. show parabolic and hyperbolic  plots on  curved spacetime using the below use curved spacetime.
\[
ds^{2}
=
-\,dt^{2}
\;+\;
\Omega(x)^{2}\,\big(dx_{1}^{2} + \cdots + dx_{n}^{2}\big),
\qquad (t,x)\in\mathbb{R}\times\mathbb{R}^{n},
\]
\[
\Omega(x)
=
\frac{1}{
\operatorname{dist}(x,\Sigma_{0})^{\alpha_{0}}\,
\operatorname{dist}(x,\Sigma_{1})^{\alpha_{1}}\,
\operatorname{dist}(x,\Sigma_{2})^{\alpha_{2}}
},
\qquad x\in\mathbb{R}^{n},
\]

\[
\Sigma_{2} \subset \Sigma_{1} \subset \Sigma_{0} \subset \mathbb{R}^{n},
\qquad
\alpha_{0},\alpha_{1},\alpha_{2} > 0.
\]


135. solve using mathdot , do not simplify show plots in the script
\[
\big(\partial_s^{4} u(s,x)\big)^{2}
\;+\;
\kappa(s)\,\big(\partial_s^{2} u(s,x)\big)^{3}
\;-\;
\det\!\big(D_x^{2} u(s,x)\big)
\;+\;
\big\|D_x^{2} u(s,x)\big\|^{2}
\;=\;
\big|u(s,x)\big|^{p-1}u(s,x),
\qquad s\in I,\; x\in\mathbb{R}^{d}.
\]

with the singular time curve
\[
\gamma(s) = \big(s^{2},\, s^{3} + s^{4}\big),
\]

and curvature
\[
\kappa(s) = 
\frac{\|\gamma'(s)\times\gamma''(s)\|}
     {\|\gamma'(s)\|^{3}},
\]

136. In the above 2 tasks The  plots need to have increased clarity and be  bigger.
provide ability to zoom in or out on the plots. do 1 doc.
136.1 Apply the above to all plots which mathdot creates. do 1 doc.
137. In both tha above tasks the notebook page does not scroll to the end,it gets stuck . Check and fix. and then do 1 doc.
138. notebook page needs to scroll some more.do 1 doc.
139. ho will you improve the graphics of the 3d plot to maximum possible,considering
youre running godot game engine. do 1 doc.
140. the screen background needs to be darker for the plot. how can you improve even more on the plot without increase in gpu. ref doc app_screenshot_task139.png
141 how will u rotate the plot in all planes. do 1 doc.
142 instead of rotate buttons, use just mouse left click and hold  to rotate. do 1 doc.
143. implement the above tasks which are still doc. do 1 doc.
144. do a thorough test of what you implemented above. do 1 doc.
145.0 push to github and do 1 doc